# perovskite-screening audit artifacts

Raw artifacts are grouped by immutable Controller cycle ID:

```text
cycles/CYCLE-000001/
cycles/CYCLE-000002/
```

Only the current cycle directory may be created or changed by an auditor. A later cycle never edits an earlier cycle.
